package app.personalfinance.data.categories;

// CategoriesTypes class, contains the types of categories
// It is hardcoded because they are parameters for the system and are not displayed on the screen.
// If you change the values, it will have an impact on the query in already populated databases.
public class CategoriesTypes {
    public static final String INCOME = "Income";
    public static final String EXPENSE = "Expense";
}
